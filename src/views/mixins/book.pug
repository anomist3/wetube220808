mixin book(book, index)
  div
    #{book.bookImg}
  li #{index + 1}
  a(href=`/books/${book.id}`) #{book.title}
  div
    small 지은이: #{book.author}
  div
    small 옮긴이: #{book.translator}
  div
    small 출판사: #{book.publisher}
  div
    small 읽은 인원: #{book.readCount}
  div
    small 토론 여부: 
      if isMeetingDone === true
        small 완료
      else
        small 대기
  div

  hr